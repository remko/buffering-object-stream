{
  "name": "buffering-object-stream",
  "version": "1.0.1",
  "description": "Buffer Node Object Streams",
  "keywords": [
    "streams",
    "buffering"
  ],
  "homepage": "https://el-tramo.be/buffering-object-stream",
  "license": "MIT",
  "author": "Remko Tronçon",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/remko/buffering-object-stream"
  },
  "scripts": {
    "test": "node_modules/.bin/tape test/**/*.js",
    "lint": "./node_modules/.bin/eslint .",
    "coverage": "node_modules/.bin/istanbul cover test/*.js"
  },
  "dependencies": {
    "through2": "^2.0.0"
  },
  "devDependencies": {
    "eslint": "^0.24.0",
    "eslint-config-remko": "^1.0.3",
    "ghooks": "^0.3.0",
    "istanbul": "~0.3.0",
    "tape": "^4.2.0"
  },
  "config": {
    "ghooks": {
      "pre-commit": "./node_modules/.bin/eslint ."
    }
  }
}
